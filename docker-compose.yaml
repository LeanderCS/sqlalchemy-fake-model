services:

  sqlalchemy-fake-model:
    build:
      context: .
      dockerfile: env_configs/Dockerfile
    container_name: sqlalchemy-fake-model
    volumes:
      - .:/app
    stdin_open: true
    tty: true
